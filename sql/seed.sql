-- seed.sql: Database initial seeding setup helper

-- Example of creating a mock admin role test entry (commented out)
-- Supabase handles admin user signups natively via Supabase Auth.
-- You can seed manual tests or leave empty.

select 'Analytics schema seeding capability initialized.' as status;
